{
  "name": "nova-studio",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "dev:studio": "turbo dev --filter=@nova-studio/studio",
    "dev:marketing": "turbo dev --filter=@nova-studio/marketing"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "packageManager": "npm@10.0.0"
}
